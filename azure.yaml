# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: wh-foundry
metadata:
  template: wh-foundry

hooks:
  postprovision:
    posix:
      shell: sh
      run: ./infra/scripts/setup-search.sh
      interactive: true
      continueOnError: false

services: {}

# before deploying the web app to App Service, ensure Entra ID authentication is set up
# to protect the app and its data.  The userId for memories can be retrieved from the Entra ID token claims, and used as the MEMORY_SCOPE for the MemorySearchPreviewTool.
  # web:
  #   project: ./web
  #   language: ts
  #   host: appservice
